{
  "name": "raffle-google",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "overrides": {
    "lru-cache": "10.4.3",
    "glob": "10.4.5"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "dev:admin": "npm run dev --workspace=admin",
    "dev:api": "npm run dev --workspace=api",
    "dev:all": "concurrently \"npm run dev:api\" \"npm run dev\" \"npm run dev:admin\"",
    "seed:db": "npm run db:migrate --workspace=api && npm run seed --workspace=api",
    "build": "npm run build --workspace=api && npm run build --workspace=web && npm run build --workspace=admin"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
